test = 123
# comment
# comment

import copy
from os import path
import urllib as test4


logger = logging.getLogger(__name__)


class HTTPHeaders(HTTPMessage):
    pass

file_type = _IOBase
zip = zip

def accepts_kwargs(func):
    pass

@classmethod
def from_dict(cls, d):
    pass

HTTPHeaders.from_dict = from_dict

try:
    hashlib.md5(usedforsecurity=False)
    MD5_AVAILABLE = True
except (AttributeError, ValueError):
    MD5_AVAILABLE = False

def _windows_shell_split(s):
    pass

try:
    import auth

    disabled = os.environ.get('XXX', "false")
    HAS_CRT = not disabled.lower() == 'true'
except ImportError:
    HAS_CRT = False

try:
    XXXX =123
finally:
    pass

LS32_PAT = "(?:{hex}:{hex}|{ipv4})".format(hex=HEX_PAT, ipv4=IPV4_PAT)
_subs = {"hex": HEX_PAT, "ls32": LS32_PAT}

try:
    import gzip
    HAS_GZIP = True
except ImportError:
    HAS_GZIP = False
    HAS_GZIP2 = True
    if aaa:
        if bbb:
            DDDDDD = 1

if xxxx:
    CCCC = 3

# comment
if True:
    ZZZ = 1
else:
    ZZZ = 2
    ZZZZ = 3

#comment
if True and aa == 1:
    if xya:
        VVV = 3
        if VVV:
            XXX = 13
    else:
        if True:
            if True:
                pass
            elif 123 and (
                    aaaa or
                    xxxx):
                if aaa:
                    SSS = "aa"
            else:
                AA = "aa"
elif fff > 10:
    BBB = 10

